connectivity_policy {
    allow_deployments_to_no_targets = true
}

versioning_strategy {
    donor_package {
        package = "seascape-core"
        step = "Update seascape-core image tags"
    }
}